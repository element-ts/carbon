/**
 * Elijah Cobb
 * elijah@elijahcobb.com
 * elijahcobb.com
 * github.com/elijahjcobb
 */

export enum CDisplayType {
	block = "block",
	none = "none",
	flex = "flex",
	inline = "inline"
}

export enum CViewPosition {
	static = "static",
	relative = "relative",
	fixed = "fixed",
	absolute = "absolute",
	sticky = "sticky"
}

export enum CBoxSizing {
	contentBox = "content-box",
	borderBox = "border-box",
	initial = "initial",
	inherit = "inherit"
}

export enum CViewOrientation {
	row = "row",
	column = "column"
}

export enum CViewPrimaryAxisPosition {
	center = "center"
}

export enum CViewSecondaryAxisPosition {
	center = "center"
}

export enum CFontWeight {
	bold = "bold",
	normal = "normal"
}

export enum CFontStyle {
	normal = "normal",
	oblique = "oblique",
	italic = "italic"
}

export enum CTarget {
	self = "_self",
	blank = "_blank",
	parent = "_parent",
	top = "_top"
}
